import 'package:flutter/material.dart';

class ActionGridHome extends StatelessWidget {
  const ActionGridHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
