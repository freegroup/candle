<BorderWidget@Widget>:
    canvas.before:
        Color:
            rgba: 1, 1, 1, 0.15  # White border; you can change the color as needed
        Line:
            rectangle: self.x, self.y, self.width, self.height
            width: 1.5  # Adjust the border width as needed
